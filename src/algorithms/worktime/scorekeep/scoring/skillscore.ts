import * as Qualifiers from '../../classes/wtqualifiers';
import { Stator } from '../../classes/globalstate';

// based on a doer's skill level and the difficulty of any task, this function will return an object with the doer's id and their skill score. this score is made up of t